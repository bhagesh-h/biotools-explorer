# =============================================================================
# BioTools Explorer — Dependency Reference
# =============================================================================
#
# This is a Node.js / TypeScript project. Dependencies are managed by npm,
# NOT pip. This file is provided as a human-readable reference.
#
# To install all dependencies, run:
#
#     npm install
#
# The authoritative dependency list is in package.json.
#
# =============================================================================

# --- System Requirements ---
# Node.js >= 18.x    (uses native fetch)
# npm     >= 9.x

# --- Runtime Dependencies (from package.json) ---

# Server
express                      # 5.0.1    — HTTP server
ws                           # 8.18.0   — WebSocket support

# Frontend Framework
react                        # 18.3.1   — UI library
react-dom                    # 18.3.1   — React DOM renderer

# Routing
wouter                       # 3.3.5    — Lightweight React router (hash-based)

# State & Data Fetching
@tanstack/react-query        # 5.60.5   — Server state management / data fetching
zod                          # 3.24.2   — Schema validation

# UI Components (shadcn/ui + Radix primitives)
@radix-ui/react-accordion    # 1.2.4
@radix-ui/react-dialog       # 1.1.7
@radix-ui/react-dropdown-menu # 2.1.7
@radix-ui/react-popover      # 1.1.7
@radix-ui/react-select       # 2.1.7
@radix-ui/react-tabs         # 1.1.4
@radix-ui/react-toast        # 1.2.7
@radix-ui/react-tooltip      # 1.2.0
# ... and other Radix primitives (see package.json for full list)

class-variance-authority     # 0.7.1    — Component variant utility
clsx                         # 2.1.1    — Class name utility
tailwind-merge               # 2.6.0    — Tailwind class merging
cmdk                         # 1.1.1    — Command palette component
vaul                         # 1.1.2    — Drawer component
embla-carousel-react         # 8.6.0    — Carousel component
react-resizable-panels       # 2.1.7    — Resizable panel layouts
input-otp                    # 1.4.2    — OTP input component

# Icons
lucide-react                 # 0.453.0  — Icon library (actions/visual cues)
react-icons                  # 5.4.0    — Brand/company logos (GitHub, etc.)

# Charts & Data Visualization
recharts                     # 2.15.2   — React charting library

# Animation
framer-motion                # 11.13.1  — Animation library

# Date Utilities
date-fns                     # 3.6.0    — Date formatting and manipulation
react-day-picker             # 8.10.1   — Calendar date picker

# Forms
react-hook-form              # 7.55.0   — Form handling
@hookform/resolvers          # 3.10.0   — Zod form validation resolver

# Theme
next-themes                  # 0.4.6    — Theme switching (light/dark)

# ORM (included but unused — no database needed)
drizzle-orm                  # 0.39.3
drizzle-zod                  # 0.7.0

# Styling
tailwindcss-animate          # 1.0.7    — Tailwind animation utilities
tw-animate-css               # 1.2.5    — Additional animation utilities

# --- Dev Dependencies ---

tailwindcss                  # 3.4.17   — CSS framework
autoprefixer                 # 10.4.20  — CSS vendor prefixes
postcss                      # 8.4.47   — CSS post-processor
typescript                   # 5.6.3    — Type checking
vite                         # 7.3.0    — Frontend bundler + dev server
@vitejs/plugin-react         # 4.7.0    — React plugin for Vite
tsx                          # 4.20.5   — TypeScript execution for server
esbuild                      # 0.25.0   — Production server bundler
drizzle-kit                  # 0.31.8   — ORM toolkit

# Type definitions
@types/node                  # 20.19.27
@types/react                 # 18.3.11
@types/react-dom             # 18.3.1
@types/express               # 5.0.0
@types/ws                    # 8.5.13

# =============================================================================
# External APIs (no packages needed — uses native fetch)
# =============================================================================
# GitHub REST API v3         — https://docs.github.com/en/rest
# Docker Hub API v2          — https://docs.docker.com/docker-hub/api/
# Europe PMC REST API        — https://europepmc.org/RestfulWebService
# =============================================================================
